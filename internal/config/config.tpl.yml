# Example multi-profile config for pvetui
profiles:
  default:
    addr: https://proxmox.local:8006
    api_path: /api2/json
    user: root
    # password: "1234567890"
    # token_id: "tui"
    # token_secret: "1234567890"
    realm: pam
    insecure: true
    ssh_user: root
    vm_ssh_user: root
    # ssh_jump_host:
    #   addr: jump.example.com
    #   user: jumpuser
    #   keyfile: /path/to/jump/key
    #   port: 2222
default_profile: default

debug: false
show_icons: true
# cache_dir: "/custom/cache/path"  # Optional: overrides platform defaults
# age_dir: "/custom/age/path"      # Optional: overrides where age keys are stored

key_bindings:
  switch_view: "]"
  switch_view_reverse: "["
  home_page: "Alt+0"
  nodes_page: "Alt+1"
  guests_page: "Alt+2"
  tasks_page: "Alt+3"
  storage_page: "Alt+4"
  tasks_toggle_queue: t
  task_stop_cancel: x
  menu: m
  global_menu: "Ctrl+g"
  shell: s
  vnc: v
  refresh: "Ctrl+r"
  auto_refresh: a
  search: "/"
  advanced_guest_filter: "Ctrl+f"
  help: "?"
  quit: q
# Reserved keys (h, j, k, l, arrows, Tab, Enter, Esc, Backspace) cannot be reassigned.
# System combos like Ctrl+C, Ctrl+D and Ctrl+Z are also blocked.

# Optional Home page integrations
# homepage:
#   nomad_addr: "http://192.168.1.202:4646"

# Theme configuration
theme:
  # Optionally set a built-in theme by name (e.g., "catppuccin-mocha").
  name: "default"
  # You can override any color from the built-in theme below.
  # colors:
    # primary: "white"
    # secondary: "gray"
    # tertiary: "aqua"
    # success: "green"
    # warning: "yellow"
    # error: "red"
    # info: "blue"
    # background: "default"
    # border: "gray"
    # selection: "blue"
    # header: "navy"
    # headertext: "yellow"
    # footer: "default"
    # footertext: "white"
    # title: "white"
    # contrast: "blue"
    # morecontrast: "fuchsia"
    # inverse: "black"
    # statusrunning: "green"
    # statusstopped: "maroon"
    # statuspending: "yellow"
    # statuserror: "red"
    # usagelow: "green"
    # usagemedium: "yellow"
    # usagehigh: "red"
    # usagecritical: "fuchsia"

plugins:
  enabled: []
  # ansible:
  #   inventory_format: "yaml"        # "yaml" (default) or "ini"
  #   inventory_style: "compact"      # "compact" (default) or "expanded"
  #   inventory_vars:                 # Optional vars injected into all.vars / [all:vars]
  #     ansible_python_interpreter: /usr/bin/python3
  #   default_user: "ubuntu"          # Optional: overrides ansible_user host var
  #   # default_password: "secret"    # Optional sensitive field (prefer SOPS/age encryption)
  #   ssh_private_key_file: "~/.ssh/id_ed25519"  # Optional default key path
  #   default_limit_mode: "selection" # "selection" (default), "all", or "none"
  #   ask_pass: false                 # Add --ask-pass by default
  #   ask_become_pass: false          # Add --ask-become-pass by default
  #   extra_args: []                  # Extra args appended to ansible/ansible-playbook
  #   env: {}                         # Extra environment variables for all ansible commands
  #     # ANSIBLE_CONFIG: "/path/to/ansible.cfg"
  #     # ANSIBLE_ROLES_PATH: "/path/to/roles"
  #     # ANSIBLE_HOST_KEY_CHECKING: "False"
  #   bootstrap:
  #     enabled: false
  #     username: "ansible"
  #     shell: "/bin/bash"
  #     create_home: true
  #     ssh_public_key_file: "~/.ssh/id_ed25519.pub"
  #     install_authorized_key: true
  #     set_password: false
  #     # password: "secret"          # Sensitive field (encrypted with age/SOPS)
  #     grant_sudo_nopasswd: false
  #     sudoers_file_mode: "0440"
  #     dry_run_default: true
  #     parallelism: 10
  #     timeout: "2m"
  #     fail_fast: false
